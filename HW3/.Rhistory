knitr::opts_chunk$set(echo = TRUE)
library(dslabs)
load('babyname')
install.packages('babynames')
#install.packages('babynames')
load('babynames')
#install.packages('babynames')
View('babynames')
#install.packages('babynames')
babynames
#install.packages('babynames')
library(babynames)
babynames
knitr::opts_chunk$set(echo = TRUE)
exams$ID[exams$improve > 0]
babynames$prop[babynames$name == "Damien" & babynames$year == 2004]
babynames[babynames$name == "Damien" & babynames$year == 2004]
babynames[babynames$name == "Damien" & babynames$year == 2004, ]
maleNames <- babyNames[babynames$sex == "M" & order(babyNames&n)[1:25],]
maleNames <- babynames[babynames$sex == "M" & order(babyNames&n)[1:25],]
maleNames <- babynames[babynames$sex == "M" & order(babynames$n)[1:25],]
femaleName <- babynames[]
maleNames <- babynames[babynames$sex == "M",]
femaleName <- babynames[]
maleNames
maleNames <- babynames[babynames$sex == "M",]
femaleName <- babynames[]
order(maleNames$n)[1:25]
maleNames <- babynames[babynames$sex == "M",]
femaleName <- babynames[]
maleNames[order(maleNames$n)[1:25],]
maleNames <- babynames[babynames$sex == "M",]
femaleName <- babynames[]
maleNames[order(maleNames$n, decreasing = TRUE)[1:25],]
maleNames <- babynames[babynames$sex == "M",]
femaleName <- babynames[babynames$sex == "F",]
maleNames <- maleNames[order(maleNames$n, decreasing = TRUE)[1:25],]
femaleNames <- femaleNames[order(femaleNames$n, decreasing = TRUE)[1:25],]
maleNames <- babynames[babynames$sex == "M",]
femaleNames <- babynames[babynames$sex == "F",]
maleNames <- maleNames[order(maleNames$n, decreasing = TRUE)[1:25],]
femaleNames <- femaleNames[order(femaleNames$n, decreasing = TRUE)[1:25],]
maleNames
femaleNames
boxplot()
maleNames <- babynames[babynames$sex == "M",]
femaleNames <- babynames[babynames$sex == "F",]
maleNames <- maleNames[order(maleNames$n, decreasing = TRUE)[1:25],]
femaleNames <- femaleNames[order(femaleNames$n, decreasing = TRUE)[1:25],]
maleNames
femaleNames
boxplot(maleNames$n ~ maleNames$name)
maleNames <- babynames[babynames$sex == "M",]
femaleNames <- babynames[babynames$sex == "F",]
maleNames <- maleNames[order(maleNames$n, decreasing = TRUE)[1:25],]
femaleNames <- femaleNames[order(femaleNames$n, decreasing = TRUE)[1:25],]
maleNames
femaleNames
boxplot(maleNames$n ~ maleNames$name)
boxplot(femaleNames$n ~ femaleNames$name)
for(x in babynames$year){
mean(babyNames$prop[year == x])
}
for(x in babynames$year){
mean(babynames$prop[year == x])
}
for(x in babynames$year){
mean(babynames$prop[babyname$year == x])
}
for(x in babynames$year){
mean(babynames$prop[babynames$year == x])
}
knitr::opts_chunk$set(echo = TRUE)
m <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
#install.packages('babynames')
library(babynames)
babynames
m <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
years <- babynames[duplicated(babynames$year) == FALSE, ]
#for(i in 1:length(m)){
#tempMatrix <- babynames[babynames$year == years$year[i],]
#m[i] = mean(tempMatrix$prop)
#}
tempMatrix <- babynames[babynames$year == years$year[1],]
tempMatrix
m <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
years <- babynames[duplicated(babynames$year) == FALSE, ]
#for(i in 1:length(m)){
#tempMatrix <- babynames[babynames$year == years$year[i],]
#m[i] = mean(tempMatrix$prop)
#}
tempMatrix <- babynames[babynames$year == years$year[1],]
mean(tempMatrix$prop)
m <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
years <- babynames[duplicated(babynames$year) == FALSE, ]
for(i in 1:length(m)){
tempMatrix <- babynames[babynames$year == years$year[i],]
m[i] = mean(tempMatrix$prop)
}
m
boxplot(babynames$year ~ m)
prop <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
years <- babynames[duplicated(babynames$year) == FALSE, ]
for(i in 1:length(m)){
tempMatrix <- babynames[babynames$year == years$year[i],]
prop[i] = mean(tempMatrix$prop)
}
prop
boxplot(years ~ m)
prop <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
years <- babynames[duplicated(babynames$year) == FALSE, ]
for(i in 1:length(m)){
tempMatrix <- babynames[babynames$year == years$year[i],]
years[i] = tempMatrix$year[1]
prop[i] = mean(tempMatrix$prop)
}
prop
years
prop <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
years <- babynames[duplicated(babynames$year) == FALSE, ]
for(i in 1:length(m)){
tempMatrix <- babynames[babynames$year == years$year[i],]
years[i] = tempMatrix$year[1]
prop[i] = mean(tempMatrix$prop)
}
#prop
years
prop <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
years <- babynames[duplicated(babynames$year) == FALSE, ]
each_year <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
for(i in 1:length(m)){
tempMatrix <- babynames[babynames$year == years$year[i],]
prop[i] = mean(tempMatrix$prop)
each_year <- tempMatrix$year[1]
}
#prop
each_year
prop <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
years <- babynames[duplicated(babynames$year) == FALSE, ]
each_year <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
for(i in 1:length(m)){
tempMatrix <- babynames[babynames$year == years$year[i],]
prop[i] = mean(tempMatrix$prop)
each_year[i] <- tempMatrix$year[1]
}
#prop
each_year
prop <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
years <- babynames[duplicated(babynames$year) == FALSE, ]
each_year <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
for(i in 1:length(m)){
tempMatrix <- babynames[babynames$year == years$year[i],]
prop[i] = mean(tempMatrix$prop)
each_year[i] <- tempMatrix$year[1]
}
prop
each_year
boxplot(each_year ~ prop)
boxplot(prop ~ each_year)
plot(prop ~ each_year)
boxplot(prop ~ each_year)
plot(prop ~ each_year)
propM <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
each_year <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
years <- babynames[duplicated(babynames$year) == FALSE, ]
for(i in 1:length(m)){
tempMatrix <- babynames[babynames$year == years$year[i],]
propM[i] = mean(tempMatrix$prop)
each_year[i] <- tempMatrix$year[1]
}
propM
each_year
plot(propM ~ each_year)
help('babynames')
YEARS <- each_year
MEAN_PROP <- propM
plot(MEAN_PROP ~ YEARS)
YEARS <- each_year
MEAN_PROPORTIONS <- propM
plot(MEAN_PROPORTIONS ~ YEARS)
propM <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
each_year <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
years <- babynames[duplicated(babynames$year) == FALSE, ]
for(i in 1:length(m)){
tempMatrix <- babynames[babynames$year == years$year[i],]
propM[i] = mean(tempMatrix$prop)
each_year[i] <- tempMatrix$year[1]
}
#propM
#each_year
propM_and_year <- data.frame(year = each_year, mean_prop = propM)
propM_and_year
with(propM_and_year, plot(years, mean_prop))
with(propM_and_year, plot(year, mean_prop))
install.packages(dplyr)
install.packages('dplyr')
#install.packages('dplyr')
tapply(distinct(babynames$year), index, mean())
#install.packages('dplyr')
tapply(distinct(babynames$year), index, mean(babynames$prop))
#install.packages('dplyr')
tapply(distinct(babynames$year), index, mean)
#install.packages('dplyr')
tapply(babynames$prop, distinct(babynames$year), mean)
#install.packages('dplyr')
require('dplyr')
tapply(babynames$prop, distinct(babynames$year), mean)
#install.packages('dplyr')
require('dplyr')
tapply(babynames$prop, unique(babynames$year), mean)
#install.packages('dplyr')
require('dplyr')
tapply(babynames$prop, babynames$year, mean)
tapply(babynames$prop, babynames$year, mean)
knitr::opts_chunk$set(echo = TRUE)
#install.packages('babynames')
library(babynames)
babynames
babynames[babynames$name == "Damien" & babynames$year == 2004, ]
#length(babynames[babynames$name == "Damien" & babynames$year == 2004, ])
maleNames <- babynames[babynames$sex == "M",]
femaleNames <- babynames[babynames$sex == "F",]
maleNames <- maleNames[order(maleNames$n, decreasing = TRUE)[1:25],]
femaleNames <- femaleNames[order(femaleNames$n, decreasing = TRUE)[1:25],]
maleNames
femaleNames
boxplot(maleNames$n ~ maleNames$name)
boxplot(femaleNames$n ~ femaleNames$name)
propM <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
each_year <- vector(length=nrow(babynames[duplicated(babynames$year) == FALSE,]))
years <- babynames[duplicated(babynames$year) == FALSE, ]
for(i in 1:length(propM)){
tempMatrix <- babynames[babynames$year == years$year[i],]
propM[i] <- mean(tempMatrix$prop)
each_year[i] <- tempMatrix$year[1]
}
#system.time(for(i in 1:length(propM)){
#tempMatrix <- babynames[babynames$year == years$year[i],]
#propM[i] <- mean(tempMatrix$prop)
#each_year[i] <- tempMatrix$year[1]
#}) # 2.14 seconds
#propM
#each_year
propM_and_year <- data.frame(year = each_year, mean_prop = propM)
propM_and_year
with(propM_and_year, plot(year, mean_prop))
tapply(babynames$prop, babynames$year, mean)
#system.time(tapply(babynames$prop, babynames$year, mean)) #about 3 second run time
